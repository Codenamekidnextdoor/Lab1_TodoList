# Project Scripts

Repository-level maintenance scripts live here. Run `npm run db:migrate` to initialize or update the configured SQLite database from the versioned files in `migrations/`.

**AI Declaration:** This document was reviewed and edited with: Github Copilot[GPT-5.6-Sol]